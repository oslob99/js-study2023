

// var line = 1;
var level = 3;

// while(line <= 9){
// console.log(`${level} x ${line} = ${level * line}`);
// line++;
// }
for(var level=2; level<=9; level++){
for (var line = 1; line <= 9; line++){
    console.log(`${level} x ${line} = ${level * line}`);
}
console.log('-------------------------------');
}